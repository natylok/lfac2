const baseUrl = "http://localhost:8877";
export const apiList = {
    login:`${baseUrl}/login`,
    register: `${baseUrl}/register`,
    getUserDetails: `${baseUrl}/1/getUser`
};
export const constants = {
    LOGIN:'login',
    REGISTER:'register'
}